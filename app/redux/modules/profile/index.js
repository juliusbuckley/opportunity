import profileReducer from './reducer';
import * as profileTypes from './types';
import * as profileActions from './actions';

export { profileTypes, profileActions };

export default profileReducer;
